export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type adressInfo={
   logradouro:string,
   bairro: string,
   cidade:string,
   estado: string
}